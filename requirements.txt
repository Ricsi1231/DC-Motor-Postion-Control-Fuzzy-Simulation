# Dependencies are declared in pyproject.toml — this file just points at them,
# so `pip install -r requirements.txt` keeps working.
#
#   pip install -e .          # runtime dependencies only
#   pip install -e '.[dev]'   # plus test, lint, and build tooling
-e .
